events{
}

http {
    server {
        listen 80;

        location /api {
            proxy_pass http://dev-backend:4000/;
            rewrite /api/(.*) /$1 break;
        }

        location / {
            proxy_pass http://dev-frontend:5000/;
        }
    }
}
